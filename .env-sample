## Copy to ".env" (optional) or export these variables in your shell.
## Required:
GITHUB_TOKEN=

## Optional:
# Also supported as an alias (if you already have it):
# github_token=
GITHUB_API_BASE_URL=https://api.github.com
GITHUB_API_VERSION=2022-11-28
GITHUB_USER_AGENT=dataset-generator/0.1
GITHUB_TIMEOUT_S=30

## Optional: sleep between paginated requests (float seconds)
GITHUB_API_SLEEP_S=0.0


